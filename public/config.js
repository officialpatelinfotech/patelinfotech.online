// Runtime configuration for the static build (cPanel-friendly).
// Update this file on the server without rebuilding the React app.
window.__APP_CONFIG__ = {
  // Set this to your Vercel API endpoint.
  // Example: "https://your-api.vercel.app/api/contact"
  CONTACT_API_URL: "",
};
